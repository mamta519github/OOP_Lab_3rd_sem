// Q.7. Write a C++ program to dynamically allocate an array of 10 elements and print the sum.

#include <iostream>
void main()
{
    int a = NULL, s = 0;
    for (int i = 0, i < 10; i++)
    {
        cin >> a[i];
    }
    for (int i = 0; i < 10; i++)
    {
        st = a[i];
    }
    cout << "sum" << s;
    delete[] = a;
    a = NULL;
    return 0;
}